# Problem Statement

HackerRank Orchestrate — Multi-Modal Evidence Review

Build an automated, multimodal claims adjudication system using Vision and Text LLMs.
The system reads `dataset/claims.csv` and produces `output.csv`.
